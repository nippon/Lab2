@using Lab2.Models.Entities
@model List<ForumThread>


@{
    ViewBag.Title = "Index";
}

<h2>Forum Index</h2>

@Html.ActionLink("Create post", "CreatePost", "Forums")

@foreach (var thread in Model ) 
{ 
    <p> @Html.ActionLink(thread.Title, "Thread/"+thread.ID)</p> //TODO:Fixa länken bättre
    <p> @thread.CreateDate </p>
   
}